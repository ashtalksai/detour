import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/layout/navbar";
import { Footer } from "@/components/layout/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Detour — Model the career nobody's parents had",
  description: "AI-powered micro-retirement simulator for millennials taking career breaks. Plan sabbaticals, gap years, and career pivots with confidence.",
  keywords: ["micro-retirement", "career break", "sabbatical planner", "financial planning", "millennials", "career pivot"],
  openGraph: {
    title: "Detour — Micro-Retirement Simulator",
    description: "Model the career nobody's parents had. Plan career breaks with AI-powered wealth projections.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
